#include <stdio.h>
#define string char*

typedef struct word {
    string word;
    int len;
} word;

string *split(string *str) {
    word tokens[100];
    for (int i = 0; i < tokens)
}

void democpu(char *binary) {
    char *tokens[] = split(binary)
}

int main() {
    int i = 0;

}